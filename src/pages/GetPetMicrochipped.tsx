import React, { useState, useEffect } from 'react';
import { Search, MapPin, AlertCircle, CheckCircle, Loader, ArrowRight, Wifi, Phone, Clock } from 'lucide-react';
import Header from "@/components/Header";
import RegisterHero1 from "@/components/RegisterHero1";
import Footer from "@/components/Footer";


  return (
    <div className="min-h-screen flex flex-col bg-white">
      <Header />
            <div className="flex-1 bg-white">
        <RegisterHero1 />
                          </div>
      <main className="flex-1 bg-white pt-20 pb-[50px]">

    <section className="flex flex-col sm:flex-row items-center justify-center gap-4 sm:gap-6 p-6">
      <button className="w-full sm:w-auto bg-blue-600 text-white font-semibold py-3 px-6 rounded-lg hover:bg-blue-700 transition-all duration-200">
        Button One
      </button>

      <button className="w-full sm:w-auto bg-gray-200 text-gray-800 font-semibold py-3 px-6 rounded-lg hover:bg-gray-300 transition-all duration-200">
        Button Two
      </button>
    </section>
  );
};

        </div>
      </main>
      <Footer />

 

export default GetPetMicrochipped;   







